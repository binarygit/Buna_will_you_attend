<!DOCTYPE html>
<html>
  <head>
    <title>AccessiblePrivateEvents</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% unless flash.empty? %>
      <ul>
      <% flash.each do |type, msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    <% end %>
    <%= yield %>
  </body>
</html>
